# Auto-load the Nix dev shell on `cd` (requires direnv + nix-direnv).
# Until direnv is installed, enter the shell manually with `nix develop`.
# This file is gitignored — personal, NixOS-specific dev convenience.
use flake
